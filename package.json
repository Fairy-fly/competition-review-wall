{
  "name": "competition-review-wall",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd backend && NODE_ENV=production node src/server.js"
  }
}
